# Repo Explorer

pnpm monorepo 기반 Tauri desktop app입니다.

## Scripts

- `pnpm dev`: Vite dev server와 Tauri dev app 실행
- `pnpm build`: desktop app frontend build
- `pnpm typecheck`: workspace TypeScript typecheck
- `pnpm tauri`: Tauri CLI 실행
